# Pin exact versions once you've confirmed a working combination on your
# GPU — these are unpinned on purpose in the scaffold since diffusers'
# Wan support is recent and moving fast; `pip install -U` right before
# first run to get the latest compatible set.
fastapi
uvicorn[standard]
pydantic
torch
diffusers
transformers
accelerate
imageio[ffmpeg]
